<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Video</title>
    <link rel="stylesheet" href="/styles.css">
    <style>
        /* You can add your styles here, or in a separate styles.css file */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4; /* Light background for body */
        }
        nav {
            background-color: #333; /* Dark background for navbar */
            overflow: hidden;
        }
        nav .navbar-logo {
            float: left;
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 1.5em;
        }
        nav .navbar-links {
            float: right;
        }
        nav .navbar-links a {
            color: white;
            text-align: center;
            padding: 14px 16px;
            text-decoration: none;
            font-size: 1em;
        }
        nav .navbar-links a:hover {
            background-color: #575757; /* Slightly lighter on hover */
        }
        nav .navbar-links a.active {
            background-color: #04AA6D; /* Green for active page */
        }
        nav .upload-btn {
            background-color: #04AA6D;
            color: white;
            padding: 10px 15px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-left: 10px;
            text-decoration: none;
        }
        nav .upload-btn:hover {
            background-color: #048751; /* Darker green on hover */
        }

        .container {
            padding: 20px;
            margin-top: 20px;
            background-color: white; /* White background for container */
            border-radius: 5px;
            width: 80%;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .container h2 {
            text-align: center;
        }

        .container form {
            display: flex;
            flex-direction: column;
            align-items: flex-start;
        }

        .container form label {
            margin-top: 10px;
            margin-bottom: 5px;
            font-weight: bold;
            display: block;
        }

        .container form input[type="text"],
        .container form input[type="number"] {
            width: calc(100% - 20px);
            padding: 10px;
            margin-bottom: 15px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }

        .container form button {
            background-color: #04AA6D; /* Green for button */
            color: white;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            margin-top: 10px;
            width: 100%;
            text-align: center;
        }

        .container form button:hover {
            background-color: #048751; /* Darker green on hover */
        }

        /* Responsive adjustments for smaller screens */
        @media screen and (max-width: 600px) {
            nav .navbar-links {
                float: none;
                display: block;
                text-align: center;
            }
            nav .navbar-links a {
                display: block;
                text-align: center;
            }
            nav .navbar-logo {
                float: none;
                text-align: center;
            }
            .container {
                width: 95%;
            }
        }
    </style>
</head>
<body>
    <nav>
        <div class="navbar-logo">My App</div>
        <div class="navbar-links">
            <a href="/" class="active">Home</a>
            <a href="/display">Videos</a>
            <a href="/playlists">Playlists</a>
            <a href="/upload" class="upload-btn">Upload Data</a>
        </div>
    </nav>

    <div class="container">
        <h2>Edit Video</h2>
        <form action="/edit/<%= video.video_id %>" method="POST">
            <label>Video Title:</label>
            <input type="text" name="video_title" value="<%= video.video_title %>" required>

            <label>Category:</label>
            <input type="text" name="category_name" value="<%= video.category_name %>" required>

            <label>Sub Category:</label>
            <input type="text" name="sub_category_name" value="<%= video.sub_category_name %>" required>

            <label>Orator:</label>
            <input type="text" name="orator" value="<%= video.orator || '' %>">

            <label>Sabha Number:</label>
            <input type="number" name="sabha_number" value="<%= video.sabha_number || 0 %>">

            <button type="submit">Update</button>
        </form>
    </div>
</body>
</html>
